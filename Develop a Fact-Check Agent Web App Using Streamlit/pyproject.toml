[project]
name = "fact-check-agent"
version = "1.0.0"
description = "Streamlit application for live-evidence PDF fact checking"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py311"
